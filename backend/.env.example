# Copy this file to .env and set your values. Do not commit .env.
# Optional: port for uvicorn (default 8000).
# PORT=8000
# Optional: comma-separated CORS origins (default allows all).
# CORS_ORIGINS=http://localhost:3000,https://your-app.com